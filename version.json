{
  "version": "2.1.2",
  "build": "2026-07-27",
  "notes": "NUZU 2.1.2 — Stops the unwanted video rotation. NUZU's tiles are 24/7 live channels and must stay put; a dormant 30-second stall-swap (old NUZU code, activated as a side effect of adding enablejsapi=1 for the audio fix) is now switched off via NZ_AUTO_SWAP=false. Tiles change ONLY on a genuine hard error (removed/private/embedding disabled). Click-to-solo audio unchanged. Service worker v15."
}
